# Frontend Tests

- `unit/`: Unit tests for helpers, stores, and small components.
- `integration/`: Integration tests for multi-component flows.

Current runnable tests are located in `src/__tests__/` to match existing npm scripts.
